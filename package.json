{
  "name": "stacklens",
  "version": "0.2.0",
  "description": "CLI for analyzing npm package dependency graphs",
  "type": "module",
  "bin": {
    "stacklens": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc",
    "test": "node --import tsx/esm --test test/**/*.test.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0"
  }
}
